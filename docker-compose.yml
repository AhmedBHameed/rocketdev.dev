version: '3.4'

volumes:
  redis_data:
    driver: local

networks:
  starlink:
    driver: bridge

services:
  rocketdev.dev:
    hostname: rocketdev.dev
    container_name: rocketdev.dev
    command: ['npm', 'run', 'start']
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./rocketdev.dev/node_modules:/home/node/node_modules
      - ./rocketdev.dev:/home/node
    ports:
      - '5000:5000'
    networks:
      - starlink
